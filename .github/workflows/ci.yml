name: Frontend CI + CodeRabbit Review (Daily @ 9AM IST)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, dev]
  schedule:
    - cron: "30 3 * * *" # 9AM IST
  workflow_dispatch:
    inputs:
      tag:
        description: "Docker image tag (for manual push)"
        required: true

env:
  DOCKER_IMAGE: allcognix/media-studio

jobs:
  # -----------------------------------
  #  Daily Email Notification (9AM)
  # -----------------------------------
  notify:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - name: Send Daily CI Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "AllCognix Frontend CI Report - ${{ github.repository }}"
          to: |
            anshul@allcognix.com,
            devops@allcognix.com,
            team@allcognix.com
          from: "AllCognix CI Bot <${{ secrets.EMAIL_USERNAME }}>"
          body: |
            Hello Team 👋,

            Here's your **Daily 9AM CI Summary** for **${{ github.repository }}**:

            🧹 Lint, Build, and Test checks running...
            🤖 CodeRabbit AI Review active on all PRs.
            🐳 Docker push available manually if required.
            ⏰ Trigger Time: ${{ github.run_started_at }}

            ---
            Automated by AllCognix CI/CD Pipeline

  # -----------------------------------
  #  Code Review (CodeRabbit AI)
  # -----------------------------------
  code-review:
    name: 🧠 CodeRabbit AI Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: CodeRabbit Info
        run: |
          echo "🚀 CodeRabbit will automatically review this Pull Request."
          echo "Ensure CodeRabbit App is installed → https://github.com/marketplace/coderabbitai"

  # -----------------------------------
  #  Lint + Build + Test
  # -----------------------------------
  build-test:
    name: 🔧 Lint, Test & Build Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Linter
        run: |
          if npm run | grep -q "^ *lint"; then
            echo "🧹 Running ESLint..."
            npm run lint -- --quiet || echo "⚠️ Lint warnings ignored."
          else
            echo "⚠️ No lint script found."
          fi

      - name: Run Tests
        run: |
          if npm run | grep -q "^ *test"; then
            echo "🧪 Running Tests..."
            npm test --if-present -- --ci --watchAll=false
          else
            echo "⚠️ No test script found."
          fi

      - name: Build Frontend
        run: |
          if npm run | grep -q "^ *build"; then
            echo "🏗️ Building frontend app..."
            npm run build
          else
            echo "⚠️ No build script found."
          fi

  # -----------------------------------
  #  Manual Docker Build & Push
  # -----------------------------------
  docker-push:
    name: 🐳 Docker Build & Push (Manual)
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    needs: [build-test]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Docker Image
        run: |
          TAG="${{ github.event.inputs.tag }}"
          echo "🐳 Building image: $DOCKER_IMAGE:${TAG}"
          docker build -t $DOCKER_IMAGE:${TAG} .
          docker push $DOCKER_IMAGE:${TAG}
          echo "✅ Image pushed: $DOCKER_IMAGE:${TAG}"
